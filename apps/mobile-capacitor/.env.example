# API base URL (same contract as Expo app)
VITE_API_URL=http://localhost:3000/api/v1

# App variant: development | preview | production
VITE_APP_VARIANT=development

# Optional: Gemini key if client-side AI is kept (prefer backend proxy)
# VITE_GEMINI_API_KEY=
